# CLAUDE.md

This project uses `AGENTS.md` as the canonical coding-agent operating contract.

Before making changes, read:

- `AGENTS.md`
- `PRD.md`
- `architecture-essentials.md`
- `architecture.md`

All implementation, testing, review, safety, AI, backend, and architectural workflow rules are defined in `AGENTS.md`.

Do not duplicate or override those rules here.

If Claude-specific behavior is required, it should be added here only when it cannot be expressed as a tool-agnostic project rule in `AGENTS.md`.
